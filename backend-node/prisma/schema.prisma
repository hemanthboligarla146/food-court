generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model analytics_event {
  id                BigInt            @id @default(autoincrement())
  event_type        String            @db.VarChar(40)
  page_path         String            @db.VarChar(255)
  search_term       String            @db.VarChar(255)
  time_on_page      Int?
  extra             Json
  created_at        DateTime          @db.Timestamptz(6)
  category_id       BigInt?
  food_id           BigInt?
  session_id        BigInt
  user_id           BigInt?
  foods_category    foods_category?   @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_event_category_id_453346d9_fk_foods_category_id")
  foods_food        foods_food?       @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_event_food_id_993168d9_fk_foods_food_id")
  analytics_session analytics_session @relation(fields: [session_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_event_session_id_96628d68_fk_analytics_session_id")
  users_user        users_user?       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_event_user_id_4b73731f_fk_users_user_id")

  @@index([event_type, created_at], map: "analytics_e_event_t_ef40a5_idx")
  @@index([food_id, event_type], map: "analytics_e_food_id_a09879_idx")
  @@index([page_path, created_at], map: "analytics_e_page_pa_df2ba0_idx")
  @@index([session_id, event_type], map: "analytics_e_session_1f7d25_idx")
  @@index([category_id], map: "analytics_event_category_id_453346d9")
  @@index([created_at], map: "analytics_event_created_at_27368550")
  @@index([event_type], map: "analytics_event_event_type_c4cbe8d6")
  @@index([event_type], map: "analytics_event_event_type_c4cbe8d6_like")
  @@index([food_id], map: "analytics_event_food_id_993168d9")
  @@index([page_path], map: "analytics_event_page_path_25db6dbd")
  @@index([page_path], map: "analytics_event_page_path_25db6dbd_like")
  @@index([session_id], map: "analytics_event_session_id_96628d68")
  @@index([user_id], map: "analytics_event_user_id_4b73731f")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model analytics_session {
  id              BigInt            @id @default(autoincrement())
  session_key     String            @unique @db.VarChar(64)
  device_type     String            @db.VarChar(16)
  user_agent      String            @db.VarChar(512)
  started_at      DateTime          @db.Timestamptz(6)
  last_seen_at    DateTime          @db.Timestamptz(6)
  user_id         BigInt?
  analytics_event analytics_event[]
  users_user      users_user?       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_session_user_id_be96f642_fk_users_user_id")

  @@index([device_type], map: "analytics_s_device__697802_idx")
  @@index([user_id, started_at], map: "analytics_s_user_id_25197d_idx")
  @@index([session_key], map: "analytics_session_session_key_cf497d89_like")
  @@index([started_at], map: "analytics_session_started_at_bd7f70bd")
  @@index([user_id], map: "analytics_session_user_id_be96f642")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model analytics_userevent {
  id             BigInt          @id @default(autoincrement())
  event_type     String          @db.VarChar(30)
  session_id     String          @db.VarChar(64)
  page_path      String          @db.VarChar(255)
  search_term    String          @db.VarChar(255)
  extra          Json
  created_at     DateTime        @db.Timestamptz(6)
  category_id    BigInt?
  food_id        BigInt?
  user_id        BigInt?
  foods_category foods_category? @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_userevent_category_id_b6a25016_fk_foods_category_id")
  foods_food     foods_food?     @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_userevent_food_id_207e64f4_fk_foods_food_id")
  users_user     users_user?     @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "analytics_userevent_user_id_e565e420_fk_users_user_id")

  @@index([event_type, created_at], map: "analytics_u_event_t_2fabd2_idx")
  @@index([food_id, event_type], map: "analytics_u_food_id_932915_idx")
  @@index([category_id], map: "analytics_userevent_category_id_b6a25016")
  @@index([created_at], map: "analytics_userevent_created_at_12b0a8ac")
  @@index([event_type], map: "analytics_userevent_event_type_47f3a280")
  @@index([event_type], map: "analytics_userevent_event_type_47f3a280_like")
  @@index([food_id], map: "analytics_userevent_food_id_207e64f4")
  @@index([session_id], map: "analytics_userevent_session_id_dfb52907")
  @@index([session_id], map: "analytics_userevent_session_id_dfb52907_like")
  @@index([user_id], map: "analytics_userevent_user_id_e565e420")
}

model auth_group {
  id                     Int                      @id @default(autoincrement())
  name                   String                   @unique @db.VarChar(150)
  auth_group_permissions auth_group_permissions[]
  users_user_groups      users_user_groups[]

  @@index([name], map: "auth_group_name_a6ea08ec_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_group_permissions {
  id              BigInt          @id @default(autoincrement())
  group_id        Int
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissio_permission_id_84c5c92e_fk_auth_perm")
  auth_group      auth_group      @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_group_permissions_group_id_b120cbf9_fk_auth_group_id")

  @@unique([group_id, permission_id], map: "auth_group_permissions_group_id_permission_id_0cd325b0_uniq")
  @@index([group_id], map: "auth_group_permissions_group_id_b120cbf9")
  @@index([permission_id], map: "auth_group_permissions_permission_id_84c5c92e")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model auth_permission {
  id                          Int                           @id @default(autoincrement())
  name                        String                        @db.VarChar(255)
  content_type_id             Int
  codename                    String                        @db.VarChar(100)
  auth_group_permissions      auth_group_permissions[]
  django_content_type         django_content_type           @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "auth_permission_content_type_id_2f476e4b_fk_django_co")
  users_user_user_permissions users_user_user_permissions[]

  @@unique([content_type_id, codename], map: "auth_permission_content_type_id_codename_01ab375a_uniq")
  @@index([content_type_id], map: "auth_permission_content_type_id_2f476e4b")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model django_admin_log {
  id                  Int                  @id @default(autoincrement())
  action_time         DateTime             @db.Timestamptz(6)
  object_id           String?
  object_repr         String               @db.VarChar(200)
  action_flag         Int                  @db.SmallInt
  change_message      String
  content_type_id     Int?
  user_id             BigInt
  django_content_type django_content_type? @relation(fields: [content_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_content_type_id_c4bce8eb_fk_django_co")
  users_user          users_user           @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "django_admin_log_user_id_c564eba6_fk_users_user_id")

  @@index([content_type_id], map: "django_admin_log_content_type_id_c4bce8eb")
  @@index([user_id], map: "django_admin_log_user_id_c564eba6")
}

model django_content_type {
  id               Int                @id @default(autoincrement())
  app_label        String             @db.VarChar(100)
  model            String             @db.VarChar(100)
  auth_permission  auth_permission[]
  django_admin_log django_admin_log[]

  @@unique([app_label, model], map: "django_content_type_app_label_model_76bd3d3b_uniq")
}

model django_migrations {
  id      BigInt   @id @default(autoincrement())
  app     String   @db.VarChar(255)
  name    String   @db.VarChar(255)
  applied DateTime @db.Timestamptz(6)
}

model django_session {
  session_key  String   @id @db.VarChar(40)
  session_data String
  expire_date  DateTime @db.Timestamptz(6)

  @@index([expire_date], map: "django_session_expire_date_a5c62663")
  @@index([session_key], map: "django_session_session_key_c0390e0f_like")
}

model foods_category {
  id                  BigInt                @id @default(autoincrement())
  name                String                @db.VarChar(100)
  image               String?               @db.VarChar(100)
  created_at          DateTime              @db.Timestamptz(6)
  analytics_event     analytics_event[]
  analytics_userevent analytics_userevent[]
  foods_food          foods_food[]
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model foods_food {
  id                  BigInt                @id @default(autoincrement())
  name                String                @db.VarChar(200)
  description         String?
  price               Decimal               @db.Decimal(10, 2)
  image               String?               @db.VarChar(100)
  is_available        Boolean
  is_featured         Boolean
  is_trending         Boolean
  created_at          DateTime              @db.Timestamptz(6)
  category_id         BigInt
  has_3d_model        Boolean
  analytics_event     analytics_event[]
  analytics_userevent analytics_userevent[]
  foods_category      foods_category        @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "foods_food_category_id_1bb11251_fk_foods_category_id")
  foods_review        foods_review[]
  orders_cart         orders_cart[]
  orders_orderitem    orders_orderitem[]
  orders_wishlist     orders_wishlist[]

  @@index([category_id], map: "foods_food_category_id_1bb11251")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model foods_review {
  id         BigInt     @id @default(autoincrement())
  rating     Int
  comment    String?
  created_at DateTime   @db.Timestamptz(6)
  food_id    BigInt
  user_id    BigInt
  foods_food foods_food @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "foods_review_food_id_04d94ca0_fk_foods_food_id")
  users_user users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "foods_review_user_id_0949569e_fk_users_user_id")

  @@index([food_id], map: "foods_review_food_id_04d94ca0")
  @@index([user_id], map: "foods_review_user_id_0949569e")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model orders_cart {
  id         BigInt     @id @default(autoincrement())
  quantity   Int
  created_at DateTime   @db.Timestamptz(6)
  food_id    BigInt
  user_id    BigInt
  size       String     @db.VarChar(10)
  foods_food foods_food @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_cart_food_id_ad92e664_fk_foods_food_id")
  users_user users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_cart_user_id_121a069e_fk_users_user_id")

  @@index([food_id], map: "orders_cart_food_id_ad92e664")
  @@index([user_id], map: "orders_cart_user_id_121a069e")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model orders_order {
  id               BigInt             @id @default(autoincrement())
  status           String             @db.VarChar(20)
  total_amount     Decimal            @db.Decimal(10, 2)
  delivery_fee     Decimal            @db.Decimal(10, 2)
  discount         Decimal            @db.Decimal(10, 2)
  address          String
  payment_method   String             @db.VarChar(50)
  created_at       DateTime           @db.Timestamptz(6)
  user_id          BigInt
  is_reviewed      Boolean
  users_user       users_user         @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_order_user_id_e9b59eb1_fk_users_user_id")
  orders_orderitem orders_orderitem[]

  @@index([user_id], map: "orders_order_user_id_e9b59eb1")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model orders_orderitem {
  id           BigInt       @id @default(autoincrement())
  quantity     Int
  price        Decimal      @db.Decimal(10, 2)
  food_id      BigInt?
  order_id     BigInt
  size         String       @db.VarChar(10)
  foods_food   foods_food?  @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_orderitem_food_id_25867428_fk_foods_food_id")
  orders_order orders_order @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_orderitem_order_id_fe61a34d_fk_orders_order_id")

  @@index([food_id], map: "orders_orderitem_food_id_25867428")
  @@index([order_id], map: "orders_orderitem_order_id_fe61a34d")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model orders_wishlist {
  id         BigInt     @id @default(autoincrement())
  created_at DateTime   @db.Timestamptz(6)
  food_id    BigInt
  user_id    BigInt
  foods_food foods_food @relation(fields: [food_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_wishlist_food_id_27d0ef41_fk_foods_food_id")
  users_user users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "orders_wishlist_user_id_557e5544_fk_users_user_id")

  @@index([food_id], map: "orders_wishlist_food_id_27d0ef41")
  @@index([user_id], map: "orders_wishlist_user_id_557e5544")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model users_address {
  id             BigInt     @id @default(autoincrement())
  title          String     @db.VarChar(50)
  street_address String     @db.VarChar(255)
  city           String     @db.VarChar(100)
  zip_code       String     @db.VarChar(20)
  is_default     Boolean
  created_at     DateTime   @db.Timestamptz(6)
  user_id        BigInt
  users_user     users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_address_user_id_4c106ba4_fk_users_user_id")

  @@index([user_id], map: "users_address_user_id_4c106ba4")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model users_paymentmethod {
  id               BigInt     @id @default(autoincrement())
  card_type        String     @db.VarChar(50)
  last_four_digits String     @db.VarChar(4)
  expiry_date      String     @db.VarChar(5)
  is_default       Boolean
  created_at       DateTime   @db.Timestamptz(6)
  user_id          BigInt
  users_user       users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_paymentmethod_user_id_2f017a1c_fk_users_user_id")

  @@index([user_id], map: "users_paymentmethod_user_id_2f017a1c")
}

model users_user {
  id                          BigInt                        @id @default(autoincrement())
  password                    String                        @db.VarChar(128)
  last_login                  DateTime?                     @db.Timestamptz(6)
  is_superuser                Boolean
  username                    String                        @unique @db.VarChar(150)
  first_name                  String                        @db.VarChar(150)
  last_name                   String                        @db.VarChar(150)
  email                       String                        @db.VarChar(254)
  is_staff                    Boolean
  is_active                   Boolean
  date_joined                 DateTime                      @db.Timestamptz(6)
  phone_number                String?                       @db.VarChar(20)
  address                     String?
  profile_picture             String?                       @db.VarChar(100)
  analytics_event             analytics_event[]
  analytics_session           analytics_session[]
  analytics_userevent         analytics_userevent[]
  django_admin_log            django_admin_log[]
  foods_review                foods_review[]
  orders_cart                 orders_cart[]
  orders_order                orders_order[]
  orders_wishlist             orders_wishlist[]
  users_address               users_address[]
  users_paymentmethod         users_paymentmethod[]
  users_user_groups           users_user_groups[]
  users_user_user_permissions users_user_user_permissions[]

  @@index([username], map: "users_user_username_06e46fe6_like")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model users_user_groups {
  id         BigInt     @id @default(autoincrement())
  user_id    BigInt
  group_id   Int
  auth_group auth_group @relation(fields: [group_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_groups_group_id_9afc8d0e_fk_auth_group_id")
  users_user users_user @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_groups_user_id_5f6f5a90_fk_users_user_id")

  @@unique([user_id, group_id], map: "users_user_groups_user_id_group_id_b88eab82_uniq")
  @@index([group_id], map: "users_user_groups_group_id_9afc8d0e")
  @@index([user_id], map: "users_user_groups_user_id_5f6f5a90")
}

/// This model has constraints using non-default deferring rules and requires additional setup for migrations. Visit https://pris.ly/d/constraint-deferring for more info.
model users_user_user_permissions {
  id              BigInt          @id @default(autoincrement())
  user_id         BigInt
  permission_id   Int
  auth_permission auth_permission @relation(fields: [permission_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_user_perm_permission_id_0b93982e_fk_auth_perm")
  users_user      users_user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "users_user_user_permissions_user_id_20aca447_fk_users_user_id")

  @@unique([user_id, permission_id], map: "users_user_user_permissions_user_id_permission_id_43338c45_uniq")
  @@index([permission_id], map: "users_user_user_permissions_permission_id_0b93982e")
  @@index([user_id], map: "users_user_user_permissions_user_id_20aca447")
}
